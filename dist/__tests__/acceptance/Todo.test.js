// import React from "react";
// import { mount, ReactWrapper } from "enzyme";
// import TodoList from "../../components/TodoList";
// import TodoLoginForm from "../../components/TodoLoginForm";
// describe.skip("Todo list", () => {
//   let todoList: ReactWrapper;
//   beforeEach(() => {
//     // todoList = mount(<TodoList tasks={() => {}}/>);
//   });
//   it("loads all blocks properly", () => {
//     expect(todoList).toHaveLength(1);
//     expect(todoList.find(TodoLoginForm)).toHaveLength(1);
//     // expect(todoList.find(TodoList)).toHaveLength(1);
//   });
//   // it.skip("shows error message on empty search value", () => {
//   //   form.find(SearchForm).prop("onSearchFieldChange")("", 0);
//   //   expect(todo.update().state("message")).toBe(ErrorMessages.emptyForm);
//   // });
// });
//# sourceMappingURL=Todo.test.js.map